<{{ values.tag is not null ? values.tag : "h2" }} class="component-title {{ values.has_decoration is not null and values.has_decoration ? "component-title--underline" : "" }} component-title--{{ values.position is not null ? values.position : "left" }} component-title--weight-{{ values.font_weight is not null ? values.font_weight : "default" }} component-title--{{ values.color is not null ? values.color : "primary" }}" style="font-family: {{ values.font is not null ? values.font : "inherit" }};font-size:{{ values.font_size is not null and values.font_size|number_format > 0 ? values.font_size|number_format ~ 'px' : "" }};">{{ values.text_content is not null ? values.text_content|raw : "" }}</{{ values.tag is not null ? values.tag : "h2" }}>
