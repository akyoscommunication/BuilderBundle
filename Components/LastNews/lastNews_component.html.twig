<div class="component-last-news {{ customClasses is defined ? ' '~customClasses : '' }}">
    {% for new in news %}
        {{ renderComponentBySlug('news', {
            'post' : new
        })|raw }}
    {% endfor %}
</div>