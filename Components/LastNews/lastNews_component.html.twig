<div class="component-last-news {{ customClasses is defined ? ' '~customClasses : '' }}">
    {% for new in news %}
        {{ renderComponentBySlug('news', {
            'post' : new
        })|raw }}
    {% else %}
        <div class="no-item w-100 text-center">Aucune actualité</div>
    {% endfor %}
</div>